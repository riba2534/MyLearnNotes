## Mysql 事务

### 情景：

多台实例运行相同代码，需要访问数据库，但是一台实例拿到数据后，就会对这部分数据进行处理，这时候不需要另一台实例也对这部分数据进行处理，所以就需要数据库进行加锁，此时需要用到MySQL的排它锁。也就是 `for update` 当机器1开始事务时，进行`for update` 操作时，机器2不能进行同样的操作，会被阻塞住，直到机器1的事务完成之后，机器2的事务才可以进行下去，这样就避免了多个机器访问同一个数据库产生的数据不一致的问题